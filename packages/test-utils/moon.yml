language: "typescript"
layer: "library"
stack: "infrastructure"
tags:
  - "test-infra"

tasks:
  typecheck:
    script: "tsc --noEmit -p tsconfig.lib.json && tsc --noEmit -p tsconfig.spec.json"

workspace:
  inheritedTasks:
    exclude: ["test"]
