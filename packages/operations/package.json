{
  "name": "@cat/operations",
  "version": "0.1.0",
  "description": "Operations layer: business workflows composing domain operations",
  "keywords": [
    "cat",
    "operations",
    "typescript"
  ],
  "license": "MIT",
  "files": [
    "dist",
    "package.json"
  ],
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "scripts": {
    "build": "vite build",
    "fmt": "oxfmt",
    "fmt:check": "oxfmt --check",
    "lint": "oxlint --type-aware",
    "lint:fix": "oxlint --fix --type-aware",
    "test": "vitest run --config ../../vitest.config.ts --project=operations",
    "test:integration": "vitest run --config ../../vitest.config.ts --project=operations --include='**/*.test.ts'",
    "test:unit": "vitest run --config ../../vitest.config.ts --project=operations --include='**/*.spec.ts'",
    "typecheck": "tsc --noEmit -p tsconfig.lib.json && tsc --noEmit -p tsconfig.spec.json"
  },
  "devDependencies": {
    "unplugin-dts": "1.0.0-beta.6",
    "vite": "8.0.0",
    "vitest": "^4.1.0"
  },
  "peerDependencies": {
    "@cat/domain": "workspace:*",
    "@cat/permissions": "workspace:*",
    "@cat/plugin-core": "workspace:*",
    "@cat/server-shared": "workspace:*",
    "@cat/shared": "workspace:*",
    "zod": "^4.3.6"
  }
}
