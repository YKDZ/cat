datasource db {
    provider = "postgresql"
    url      = "postgresql://user:pass@localhost:8654/cat?schema=public"
}

generator client {
    provider        = "prisma-client"
    previewFeatures = ["fullTextSearchPostgres"]
    output          = "../src/generated/prisma"
    moduleFormat    = "esm"
}