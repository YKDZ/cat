language: "typescript"
layer: "application"
stack: "infrastructure"
tags:
  - "cli"
  - "deployable"

tasks:
  build:
    script: "vite build"

  typecheck:
    script: "tsc --noEmit -p tsconfig.lib.json"
