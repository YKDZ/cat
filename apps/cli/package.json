{
  "name": "@cat/cli",
  "version": "0.0.1",
  "private": true,
  "bin": {
    "cat-cli": "./dist/cli.js"
  },
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "test": "vitest run --config ../../vitest.config.ts --project=unit-cli",
    "test:unit": "vitest run --config ../../vitest.config.ts --project=unit-cli --include='**/*.spec.ts'"
  },
  "dependencies": {
    "@orpc/client": "^1.14.0",
    "@orpc/server": "^1.14.0"
  },
  "devDependencies": {
    "@types/node": "catalog:",
    "typescript": "beta",
    "unplugin-dts": "catalog:",
    "vite": "catalog:",
    "vitest": "catalog:"
  },
  "peerDependencies": {
    "@cat/app-api": "workspace:*"
  }
}
