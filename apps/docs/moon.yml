language: "typescript"
layer: "application"
stack: "frontend"
tags:
  - "docs"

tasks:
  build:
    command: "vitepress build src"

  dev:
    command: "vitepress dev src"
    preset: "server"

  preview:
    command: "vitepress preview src"
    deps:
      - "~:build"
    options:
      persistent: true

  typecheck:
    script: "tsc --noEmit -p tsconfig.app.json && vue-tsc --noEmit -p tsconfig.app.json"
    deps:
      - "^:build"
      - "autodoc:build"

workspace:
  inheritedTasks:
    exclude: ["test"]
