import type { PreCheckServices } from "@cat/agent";

/**
 * Create the PreCheck service set required by AgentRuntime.
 *
 * @returns - PreCheck services injectable into AgentRuntime
 */
export const createPreCheckServices = (): PreCheckServices => ({});
